package com.ushareit.query.utils;

/*import com.ushareit.interceptor.model.RoleVO;
import com.ushareit.interceptor.service.IRemoteService;
import com.ushareit.query.constant.CommonConstant;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import java.util.List;*/

/**
 * @program: hebe
 * @description:
 * @author: wuyan
 * @create: 2020-05-08 14:37
 **/
/*@Slf4j
@Component
public class ZeusUtil {
    @Resource
    private IRemoteService iRemoteService;

    public Boolean isAdmin(HttpServletRequest request) {
        List<RoleVO> userRoleList = roleList(request);
        for (RoleVO userRole : userRoleList) {
            if (CommonConstant.ADMIN.equals(userRole.getType())) {
                return true;
            }
        }
        return false;
    }

    public List<RoleVO> roleList(HttpServletRequest request) {
        return  iRemoteService.getUserRole(request);

    }
}*/
